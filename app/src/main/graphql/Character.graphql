query Character($id: ID!){
    character(id: $id){
        id,
        name,
        status,
        species,
        image,
        origin {
            name
        },
        location {
            name
        }
    }
}